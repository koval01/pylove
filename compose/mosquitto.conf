# Mosquitto for LovensePy + Home Assistant Docker stack (internal network).
# Mosquitto 2.x: set protocol per listener when using a custom config.
listener 1883 0.0.0.0
protocol mqtt
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
